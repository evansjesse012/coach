import Foundation

/// SECTION 9 — Goal router & specialty lenses.
///
/// Phase 5 authoring target (~1800 tokens — the second-largest section).
/// Six specialty sketches (endurance / hypertrophy / strength / fat loss /
/// hybrid / general fitness), an explicit "first identify the lens"
/// routing protocol, and when-to-apply gates per lens.
///
/// Phase 1: empty. The original prompt has no goal-routing or specialty
/// content — the model improvises a framework per turn from
/// pre-training. Decision #1 commits us to encoding our specific lenses
/// here so the prescription stays consistent across conversations.
enum Section09_GoalRouter {
    static let content: String = ""
}
