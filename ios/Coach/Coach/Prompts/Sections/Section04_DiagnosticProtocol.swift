import Foundation

/// SECTION 4 — Diagnostic protocol.
///
/// Phase 5 authoring target (~400 tokens): how to figure out what the
/// athlete actually needs before prescribing. Discovery questions when
/// the goal is ambiguous; what NOT to ask (don't double-fetch data
/// already in the state pack); how many questions are too many.
///
/// Phase 1: empty. No equivalent in original prompt.
enum Section04_DiagnosticProtocol {
    static let content: String = ""
}
