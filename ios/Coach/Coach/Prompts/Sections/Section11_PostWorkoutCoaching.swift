import Foundation

/// SECTION 11 — Post-workout coaching.
///
/// Phase 5 authoring target (~500 tokens): how the coach handles the
/// post-status check-in, what to look for, what to ask, when to push
/// vs acknowledge. Coordinates with the iOS-side `PostStatusChatSheet`
/// flow that already pre-populates a framed user message into the
/// thread when an athlete logs a status.
///
/// Phase 1: empty. The original prompt has no post-workout-specific
/// guidance — the coach handles each session log as a generic chat turn.
enum Section11_PostWorkoutCoaching {
    static let content: String = ""
}
