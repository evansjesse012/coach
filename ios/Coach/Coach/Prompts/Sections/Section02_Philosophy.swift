import Foundation

/// SECTION 2 — Coaching philosophy. (Highest-leverage section.)
///
/// Phase 5 authoring target (~900 tokens): the commitments that constrain
/// every decision. What this coach believes about progressive overload,
/// recovery, specificity, scale-of-evidence, when to push vs hold,
/// what counts as data, what doesn't. Encodes the POV Claude can't
/// infer from training data alone.
///
/// Phase 1: empty. The original prompt has no philosophy section.
enum Section02_Philosophy {
    static let content: String = ""
}
