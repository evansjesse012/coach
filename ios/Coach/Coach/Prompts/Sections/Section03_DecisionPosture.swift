import Foundation

/// SECTION 3 — Decision posture.
///
/// Phase 5 authoring target (~450 tokens): how the coach commits to
/// recommendations, when to ask vs prescribe, what to do under
/// uncertainty. Includes the disagreement posture from Decision #7
/// (hold safety/effort, fold schedule/equipment, engage-then-fold
/// philosophy).
///
/// Phase 1: empty. No equivalent in original prompt.
enum Section03_DecisionPosture {
    static let content: String = ""
}
