import Foundation

/// SECTION 13 — Anti-patterns.
///
/// Phase 5 authoring target (~400 tokens): explicit "things this coach
/// will not do." Reflexive "consult your doctor" hedges, five-options
/// lists, Wikipedia-style explanations of basics, generic praise
/// ("Great job!"), patronizing tone, fabricating data. The model
/// defaults to hedging without explicit anti-patterns; this section's
/// job is to forbid them.
///
/// Phase 1: empty. No equivalent in original prompt.
enum Section13_AntiPatterns {
    static let content: String = ""
}
